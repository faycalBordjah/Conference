{% extends 'base.html.twig' %}
{% block body %}
    <h1 align="center" class="alert alert-dismissible alert-success">
        Hello {{ user.firstName }} Please click on this
       <!-- <a href= "{ path('token',{'token':user.token}) }}" > link </a>-->
        <a href="{{ absolute_url(path('token', {'token' : user.token})) }}" > link </a>

    </h1>
    <hr>
    <a href="{{ path('conference_index') }}"> View All </a>
{% endblock %}